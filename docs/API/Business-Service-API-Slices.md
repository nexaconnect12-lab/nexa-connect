# Business Service API Slices

Restaurant customer branch management uses `restaurant.branch.read` and `restaurant.branch.manage`. Restaurant combines a coarse customer owner/admin realm role, active Platform Directory access, an Authorization decision, and organization-leading persistence predicates. The Customer BFF derives organization from its protected tenant selection.

The former weather scaffold endpoints have been replaced with initial bounded-context APIs. All endpoints use the shared authenticated API policy.

| Service | Routes | Current persistence |
| --- | --- | --- |
| Catalog | `GET` and `POST /api/catalog/v1/branches/{branchId}/menu-items` | PostgreSQL menu, append-only audit, and transactional outbox state; otherwise non-durable in-memory. Outbox dispatch is optional. |
| Inventory | `GET /api/inventory/v1/branches/{branchId}/stock`, `PUT .../stock/{productId}`, `POST .../reservations` | PostgreSQL stock/reservations and durable inbox; tenant-scoped PostgreSQL writes also atomically append product audit and outbox records. Trusted unscoped paths remain legacy/non-publishing; otherwise persistence is non-durable in-memory. |
| Kitchen | exact-Order ticket create/cancel; tenant/operator branch read and transition routes | PostgreSQL ticket/items, append-only history/audit, and transactional outbox; otherwise non-durable in-memory |
| Order | `POST` and `GET /api/order/v1/orders`; `POST /api/order/v1/workflows/place` | PostgreSQL aggregate, idempotency, and transactional outbox when `Persistence:Provider=PostgreSQL`; otherwise in-memory |
| Payment | `POST` and `GET /api/payment/v1/intents` | PostgreSQL tenant-owned intents, append-only audit, and transactional outbox; otherwise non-durable in-memory. Organization/restaurant idempotency rejects conflicting reuse. |
| Customer | `POST /api/customer/v1/organizations/{organizationId}/customers`; `GET .../customers/{id}` | PostgreSQL profile/audit/two-event transaction and conflict-safe replay when `Persistence:Provider=PostgreSQL`; otherwise non-durable in-memory |
| Notification | `POST /api/notification/v1/notifications`; `GET /api/notification/v1/notifications/{id}`; Customer BFF `GET /bff/customer/notifications/{id}` | PostgreSQL notification, audit, inbox/outbox, leased provider lifecycle, and append-only delivery attempts; otherwise non-durable in-memory state |
| Platform Directory | `GET /api/platform-directory/v1/me/access`, `GET /api/platform-directory/v1/organizations/{organizationId}/access` | Organization membership and enabled product-access boundary; product authorization remains owned by each product |
| Restaurant | Branch management and `GET/PUT .../configuration/branches/{branchId}` | PostgreSQL hierarchy, typed branch configuration, and append-only audit |
| Reporting | `GET .../dashboard`, `GET .../reports/sales`, `GET .../activity` | PostgreSQL event projections only; activity uses cursor pagination |
| Media | list, upload-start/complete, signed original/variant download, delete | PostgreSQL metadata, quotas, expiry cleanup, ClamAV inspection, S3-compatible lifecycle, and durable WebP generation |

Customer Portal Catalog reads use `X-Nexa-Portal-Request: customer`, `X-Nexa-Organization-Id`, and `X-Nexa-Application-Code: nexa_connect` headers. The Catalog service validates organization access through Platform Directory using the forwarded customer bearer token, then checks the selected branch's Restaurant-owned authorization scope with the Catalog workload identity. A branch whose scope organization does not match the selected organization is rejected; the headers remain context, not a substitute for authorization.

The Customer BFF exposes explicit configuration, dashboard, sales-report, activity, media, and notification-read routes whose organization is derived from the protected tenant selection. No capability remains on the availability placeholder route.

All implemented customer-tenant operations also obtain an Authorization service decision. Permission codes include the existing catalog, inventory, order, payment, and customer profile permissions plus `restaurant.branch.read/manage`, `restaurant.configuration.read/manage`, `reporting.dashboard.read`, `reporting.sales.read`, `media.asset.read`, `kitchen.ticket.read`, and `kitchen.ticket.transition`. Only allow-listed service workload tokens identified by their validated `azp` claim may use internal paths without tenant context; ordinary authenticated users fail closed when context is absent or conflicting. Resource reads generally return `404` to avoid disclosure, authorization denials return `403`, and malformed Catalog context returns `400`.

`POST /api/authorization/v1/role-assignments` provisions the permission set for the selected product role. `tenant-admin` uses organization scope, `store-manager` uses restaurant scope, and cashier, inventory-controller, accountant, and report-viewer use branch scope. The first two receive the full implemented tenant-API set; the other roles receive narrower role-appropriate sets. The operation requires `system-admin`, `platform-owner`, or `platform-admin`; hierarchical matching never crosses the required organization predicate.

Platform owners and administrators provision Restaurant scope through `POST /api/restaurant/v1/restaurants` and `POST /api/restaurant/v1/restaurants/{restaurantId}/branches`, normally through the matching Platform Admin BFF routes. Restaurant and Authorization remain the persistence owners; the BFF performs no database writes.

See [Restaurant Provisioning](Restaurant-Provisioning.md) for request, response, validation, status, idempotency, and ownership details.

See [Customer product configuration, reporting, and media](Customer-Product-Configuration-and-Reporting.md) for exact range, currency, checkpoint, cap, concurrency, and failure contracts.

Inventory operations tagged as Customer Portal requests validate active organization access through Platform Directory, confirm the route branch belongs to that organization through Restaurant, and require the operation-specific permission. Internal service calls remain supported without customer portal headers.

Payment intent create and read require `X-Nexa-Organization-Id`. Customer Portal requests also validate active organization access, resolve the referenced Order with the Payment workload identity, require the Order organization and branch to match the protected tenant context, and confirm the Restaurant-owned restaurant/branch scope. Only the exact `nexaconnect-order-service` trusted workload bypasses customer authorization; other shared trusted workloads are denied. Order propagates organization/application headers from the aggregate rather than static deployment configuration. PostgreSQL reads are organization-leading. A repeated matching idempotency request returns the original intent without new audit/events; conflicting reuse returns `409`.

`POST /api/payment/v1/intents/{id}/authorize` is an internal Order-workload operation. It returns `200 authorized` after approval, `200 failed` for a definitive decline/failure, or an uncertain `200 authorizing`, `200 unknown`, or `200 requires_action`; these are business states in the response body, not HTTP failures. A stale completion conflict returns `409`, missing or cross-organization intents return `404`, and non-Order callers receive `403`. Payment commits an `authorizing` lease before provider I/O and commits the sanitized result afterward. Order compensates only definitive failure and retains work for uncertainty.

`POST /api/payment/v1/intents/{id}/capture` is also exact-Order-workload-only. The initial transition requires `authorized`; replays in `captured`, `capturing`, or `capture_unknown` return the current state without another provider call. It returns `200 captured`, `200 failed`, `200 capturing`, or `200 capture_unknown`, and uses `404`/`403`/`409` for disclosure-safe absence, caller denial, or illegal/stale transitions. The place-order adapter invokes capture immediately after authorization and treats only `captured` as paid. PostgreSQL capture recovery claims uncertain/expired work, looks up status by the stable intent identity, and publishes `PaymentCaptureReconciledV1`; it never repeats the capture command. `capture_unknown` and exhausted `requires_action` retain the Order in `PaymentPending`; only definitive failure permits compensation. `POST /api/payment/v1/intents/{id}/void` is exact-Order-workload-only and supported by PostgreSQL and in-memory adapters for authorized, uncaptured intents. It can return `voided`, `voiding`, `void_failed`, `void_unknown`, or exhausted `requires_action`; replays of established non-failed states do not repeat the provider command. Captured payments return `409` because they require refunds. Payment migration 7 persists recovery state and transactional lifecycle/audit events; the worker resolves uncertainty through provider status lookup. Provider references remain internal and are not serialized in public responses.

Kitchen routes are:

| Route | Required context | Contract and status behavior |
| --- | --- | --- |
| `POST /api/kitchen/v1/tickets` | Order workload bearer token with exact `azp=nexaconnect-order-service`; `X-Nexa-Organization-Id` | Body: `{ restaurantId, orderId, branchId, lines: [{ productId, name, quantity, preparationStation }] }`. Returns `201`; malformed snapshots return `400`; conflicting station identity reuse returns `409`; other workloads return `403`. |
| `POST /api/kitchen/v1/tickets/{orderId}/cancel?branchId={branchId}` | Exact Order workload and organization header | Returns `204`, `404` when no matching ticket exists, `400` for missing context, `409` when a terminal transition prevents compensation, and `403` for other workloads. |
| `GET /api/kitchen/v1/branches/{branchId}/tickets/{ticketId}` | Customer bearer token, `X-Nexa-Organization-Id`, `X-Nexa-Application-Code: nexa_connect`; `kitchen.ticket.read` | Returns `200` or disclosure-safe `404` for absent, cross-tenant, cross-branch, or unauthorized tickets. |
| `POST /api/kitchen/v1/branches/{branchId}/tickets/{ticketId}/transitions` | Same tenant headers; `kitchen.ticket.transition` | Body: `{ targetStatus, expectedConcurrencyVersion, reasonCode? }`, where `targetStatus` is `Queued`, `InProgress`, `Ready`, `Completed`, or `Cancelled`. Returns `200`, `400` for malformed input, `403` for missing context/scope or denial, `404` for a missing ticket, and `409` for stale or illegal transitions. A same-target request with the current version is a no-op. |

Kitchen validates organization access through Platform Directory, resolves Restaurant branch ownership using the `nexaconnect-kitchen-service` workload identity, and obtains the scoped Authorization decision. `tenant-admin` and `store-manager` permission backfill is migration-managed. `X-Correlation-ID` is propagated to dependencies and preserved in Kitchen outbox/event data.

Customer profile routes require `X-Nexa-Organization-Id` to match the route, `X-Nexa-Application-Code: nexa_connect`, a customer bearer subject, active Platform Directory access, and `customer.profile.create` or `customer.profile.read`; workload identities have no unscoped bypass. Missing or invalid authentication returns `401`. `POST` accepts `{ customerNumber, displayName, identitySubjectId? }`, returns `201` for first creation or matching replay, `400` for invalid bounded fields, `403` for missing/denied tenant context or a fail-closed authorization dependency response, and `409` when the normalized organization/customer-number identity or optional identity subject is already associated with different profile data. PostgreSQL reads retain the organization predicate, so a resource identifier alone can never select another tenant's profile; absent, anonymized, cross-tenant, and unauthorized reads return `404`. PostgreSQL migration 2 atomically appends profile-field-excluding audit and `customer.profile-created.v1`/`customer.audit.v1` outbox rows only for first creation. Audit actor subjects are restricted pseudonymous accountability identifiers and are never emitted to operational logs.

The Customer BFF exposes `POST /bff/customer/orders/branches/{branchId}/place`. It derives `OrganizationId` from the protected tenant selection and `BranchId` from the route, forwards the server-held bearer token and tenant headers to Order, and accepts only the customer order fields (`RestaurantId`, `Currency`, `PaymentMethod`, `IdempotencyKey`, `Lines`, with optional `OrderId` and `CorrelationId`). Browser-supplied organization or branch fields are not trusted.

Catalog, Inventory, and Payment use PostgreSQL adapters when `Persistence:Provider=PostgreSQL` and otherwise use their non-durable in-memory adapters. Payment migrations 2-7 add tenant ownership, audit/outbox publication, authorization/capture, and durable void recovery; Reporting migrations 8-12 must precede the corresponding publication. Notification migration 2 supplies organization/source-event scoping, product audit, inbox deduplication, and a transactional outbox; migration 3 adds provider delivery leases, retries, receipts, and append-only attempt history. Order migration 2 owns its durable reconciliation inbox and pending status; migration 3 adds `payment_review`. Confirmed void compensates unpaid work, uncertain void retains it, definitive failure/exhaustion enters review, and paid orders ignore void events. No controller contains SQL or direct persistence access.

Catalog migration 1 owns the shared-format outbox table. Migration 4 adds append-only Catalog audit storage, while application writes transactionally record each PostgreSQL menu-item upsert with `catalog.menu-item.changed.v1` and `catalog.audit.v1`. The menu event contains the commercial and preparation snapshot required by downstream consumers; the audit event contains identifiers and outcome only. The rows are committed whether or not dispatch is enabled. In PostgreSQL mode, `Outbox:Enabled=true` starts the shared RabbitMQ dispatcher and requires `Outbox:ConnectionString`.

Notification tenant calls require a matching organization/application context, `notification.send` or `notification.read`, active Platform Directory access, and an Authorization decision; there is no broad workload bypass. Public create accepts only organization, channel, recipient, subject, and body, returning `201` with `queued` state; malformed fields return `400` and denied context returns `403`. `NotificationRequestedV1` is the asynchronous workload contract and `NotificationQueuedV1` confirms durable acceptance. Its consumer uses a lease-based inbox plus a globally unique source event; matching replay returns the original row while conflicting or cross-organization reuse is rejected. PostgreSQL provider processing emits safe `notification.delivery-status-changed.v1` accepted/delivered/failed events. Recipient, subject, body, provider payloads, and credentials must never be written to operational logs, audit, or integration events.

`POST /api/order/v1/workflows/place` accepts `RestaurantId`, `OrganizationId`, `BranchId`, `Currency`, `PaymentMethod`, a required `IdempotencyKey`, and one or more `{ ProductId, Quantity }` lines. It returns `200 OK` for a completed workflow, `409 Conflict` for rejected/payment-failed orders, `400 Bad Request` for invalid input, and `422 Unprocessable Entity` when a menu, inventory, kitchen, or payment step cannot be completed. In PostgreSQL mode, repeating the same restaurant/key returns the previously persisted order result without replaying the workflow. The HTTP adapters use the configured workload bearer-token provider and retry transient dependency failures; production deployments must configure their service URLs and workload credentials. Customer Portal calls through `/bff/customer/orders/branches/{branchId}/place` derive organization and branch context from the protected BFF tenant selection and route. Order independently revalidates organization access through Platform Directory and branch ownership through Restaurant before running the workflow.

Authenticated Order payment-review operations are exposed at `GET /api/order/v1/payment-reviews`, `GET /api/order/v1/payment-reviews/{orderId}`, and `POST /api/order/v1/payment-reviews/{orderId}/resolve`. Unauthenticated calls receive `401`. Reads require `order.payment-review.read`; resolution requires `order.payment-review.resolve`, matching tenant header, organization, and Restaurant-owned branch scope. Resolution accepts `confirm_void`, `resume_payment`, or `escalate`, a bounded reason, and the expected concurrency version. Stale decisions return `409`; cross-tenant reads remain undiscoverable. The API never exposes provider payloads or payment credentials.

Payment-review routes have no trusted-workload bypass. Resolution persists the Authorization decision ID and uses a two-minute fenced lease; active claims reject competing decisions, while an expired claim can be retried only with the same resolution.
