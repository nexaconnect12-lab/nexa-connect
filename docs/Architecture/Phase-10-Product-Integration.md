# Phase 10 — Product service integration

The [five-scenario hosted Omise test-account gate passed on 2026-09-18](Evidence/Omise-Five-Scenario-Recovery-Acceptance.md), including pre-authorization token handoff. Payment implements opt-in signed test webhooks with migration-8 durable event-ID ingestion, authenticated canonical event validation and lease-fenced status-only recovery. The guarded real external-delivery/process-interruption gate passed on 2026-09-22, covering the disposable Testing-only post-commit/pre-acknowledgement interruption and signed duplicate replay; production enablement remains open. See [configuration and live acceptance](../Deployment/Omise-Webhooks.md).

Opt-in Development-only `card_omise_test` checkout now forwards a masked, transient test token from POS to Order and Payment. Pending intents without a token await a fresh operator token; retries use the original Order identity and contents. Tokens never enter SQLite/outbox or durable workflow state. The opt-in five-scenario hosted Omise gate passed on 2026-09-18, including pre-authorization/token handoff. [Local POS Omise verification passed on 2026-09-18](Evidence/Omise-POS-Card-Acceptance.md) with operator-attested UI/OIDC/sign-out; the verifier does not independently inspect those interactions. Production browser card collection remains planned. See the [configuration and acceptance guide](../Deployment/Omise-POS-Card-Token-Handoff.md).

Payment also has a test-account-only Omise THB card adapter with ephemeral authorization tokens and metadata-bound status recovery. Local contract tests cover the adapter and HTTP authorization boundary; direct credentialed Omise acceptance passed on 2026-09-17, including signed-context retention, authorization reference-loss lookup and capture/reversal replay/status. The [four-scenario hosted Omise harness](../Deployment/Omise-Hosted-Recovery-Acceptance.md) is implemented with initial test-token fixtures, real hosted recovery and outbox restart; credentialed four-scenario execution passed on 2026-09-17. The historical four-scenario run excludes interruption before authorization; the subsequent opt-in five-scenario gate passed on 2026-09-18; see [retained direct acceptance evidence](Evidence/Omise-Test-Account-Acceptance.md). production card collection/independent proof of UI interaction, 3DS, PromptPay, refunds, settlement and production activation are separate work. See [Omise scope and acceptance](../Deployment/Omise-Test-Account-Acceptance.md).


See [retained four-scenario hosted Omise evidence](Evidence/Omise-Hosted-Recovery-Acceptance.md) for the exact pass and exclusions.

The provider recovery gate now includes hosted pre-capture void recovery, duplicate delivery and paid-Order protection. Its five-scenario local hosted acceptance passed on 2026-09-17, including duplicate void delivery and cleanup; four-scenario hosted Omise execution passed on 2026-09-17; five-scenario Omise pre-authorization acceptance passed on 2026-09-18; production acceptance remains open. See [recovery scope, evidence and remaining release gates](Evidence/Phase-10-Order-Workflow-Recovery.md).

Phase 10 is delivered as product-owned vertical slices. A service completes the phase only when its own Application ports, tenant/resource authorization, versioned API/BFF boundary, integration contracts, idempotency boundary, transactional publication, PostgreSQL persistence, reversible migration tests, and safe audit events are implemented where its workflows require them. “Not applicable” must be justified by the service's role; it is not permission to share another service's domain model or database.

Shared projects may contain immutable integration-event DTOs, tenant header names, permission-code constants, and low-level messaging infrastructure. They must not contain product aggregates, entities, repositories, or database models.

## Current integration matrix

The provider interruption harness now has a self-contained local HTTPS simulator wrapper requiring no external provider credentials. The complete local hosted matrix passed on 2026-09-16 after fixing the evidence subscription and RabbitMQ restart persistence, including all three provider-boundary and Payment-host interruptions plus cleanup. Four-scenario hosted Omise execution passed on 2026-09-17; five-scenario Omise pre-authorization acceptance passed on 2026-09-18; production acceptance remains a separate release gate. See the [simulator guide](../../src/Tools/NexaConnect.PaymentProviderSimulator/README.md).

| Product service | Current Phase 10 foundation | Remaining exit work |
| --- | --- | --- |
| Catalog | Product-owned menu interfaces and PostgreSQL repository; tenant/branch authorization; API and Customer BFF contract; `CatalogMenuItemChangedV1`; append-only product audit; transactional outbox; paired migration 4 scripts; opt-in live PostgreSQL atomicity/rollback/trigger/migration coverage; implemented full-database 0→4→3→4 runner acceptance; real RabbitMQ acceptance for an unreachable connection attempt, a commit without a broker connection, and later confirmed publication over a new connection with persistent messages, isolated consumption, and publication timestamps | Closed for the Phase 10 product-integration slice; retain a successful production-like full migration acceptance run as a release gate; established-dispatcher reconnection is not covered by this acceptance |
| Customer | Product-owned profile aggregate/repository; organization authorization and API contract; conflict-safe concurrent creation replay; organization-leading PostgreSQL; append-only audit that excludes profile fields; migration-1 outbox; transactional `customer.profile-created.v1`/`customer.audit.v1`; Reporting migration-6 compatibility. Six coordinated PostgreSQL 17/RabbitMQ acceptances passed locally, including concurrent replay, atomic rollback, confirmed publication, Reporting replay, and 0→2→1→2. The shared outbox transport now replaces failed owned connections, though this service-specific acceptance does not exercise that path. | Closed for the profile-creation slice; status transitions, contacts, addresses, preferences, and loyalty remain future slices |
| Inventory | Product-owned stock/reservation interfaces; branch authorization; PostgreSQL repository; durable inbox; versioned stock/reservation/release contracts; persisted reservation identity and concurrent active-order idempotency; tenant-scoped transactional publication through the migration-1 outbox; append-only audit migration 5. All seven opt-in acceptances passed locally against PostgreSQL 17 and RabbitMQ, including confirmed persistent publication of all three events and the actual migration runner's 0→5→4→5 lifecycle. Trusted unscoped paths remain legacy/non-publishing. | Closed for the Phase 10 product-integration slice; established-dispatcher reconnection is not covered by the broker acceptance |
| Kitchen | Ticket lifecycle invariants; exact Order workload create/cancel; dedicated Kitchen workload identity for authorization-scope lookup; Authorization migration-3 role-permission backfill; tenant-authorized operator read/transition; organization-leading PostgreSQL; conflict-safe station snapshots; multi-station identity; append-only history/audit; migration-1 outbox; lifecycle/audit events; Reporting migration-5 compatibility. Five coordinated acceptances passed against PostgreSQL 17 and RabbitMQ, including 0→3→2→3 and replay. The shared outbox transport now replaces failed owned connections, though this service-specific acceptance does not exercise that path. | Closed for the ticket-level slice; canonical station IDs, item transitions, queues/KDS, adjustments, and offline execution remain future work |
| Media | Product-owned repositories/workers; tenant authorization; Customer BFF/API contracts; PostgreSQL lifecycle; outbox/audit activity publication; retry-safe claimed jobs | Maintain provider/component rollback and recovery evidence for each release |
| Notification | Product-owned validation and delivery policy; tenant-only direct API and Customer BFF read contract; `NotificationRequestedV1`/`NotificationQueuedV1`; inbox plus conflict-safe source-event deduplication; transactional queue/lifecycle audit and outbox; lease recovery; idempotent provider submission; bounded retries and receipt polling; append-only attempts; Notification migration 3 and Reporting migration 7. Eight coordinated PostgreSQL 17/RabbitMQ 4 acceptances passed locally, including lifecycle rollback, lease recovery, confirmed publication, the actual 0→3→2→3 runner, and Reporting replay. | Closed for the provider-delivery lifecycle slice; add approved producers only after recipient/preferences can be resolved without copying Customer data; production acceptance against each concrete provider and signed webhook support remain provider-specific release/future work |
| Order | Domain aggregate and Application workflow ports; tenant/branch authorization; Customer BFF/API; versioned workflow events; idempotency; PostgreSQL aggregate and transactional outbox; migrations 2-3 durable capture/void reconciliation; migration-4 fenced Payment Review decisions, immutable history and atomic audit/events; migration-5 exact manual-tender settlement; migration-6 precise pre-payment stages and claim/lease recovery of interrupted manual-tender Inventory/Kitchen work; migration-7 provider-payment scheduling, stable Payment-intent resumption, durable reconciliation handoff, and review on exhausted uncertainty; guarded manual-tender process-interruption acceptance passed two kill/restart boundaries, dependency retry, correlation, exact transition, final-settlement, and cleanup checks on 2026-09-15; guarded concrete-provider runner covers three server-side interruption boundaries, real Order/Payment hosts, hosted Payment recovery, broker-outage outbox persistence, Payment restart, publication, and Order inbox consumption; Customer Portal branch-scoped list/detail/history with CSRF-protected confirmation through a tenant-revalidated BFF | Joined live identity/BFF/Order operator acceptance and production acceptance remain required (five-scenario Omise hosted acceptance, including pre-authorization token handoff, passed on 2026-09-18) (the complete local hosted-simulator matrix passed on 2026-09-16); extend product audit coverage as return/refund workflows are implemented and retain live duplicate/redelivery plus partial-compensation recovery evidence as release gates |
| Payment | Product-owned intent validation; exact Order-workload authorization and immediate capture; organization-leading persistence; conflict-safe idempotency; authorization and capture recovery; provider calls outside transactions; sanitized outcomes; append-only audit; migration-1 outbox; migrations 4-8 lifecycle/recovery and webhook inbox state; transactional reconciliation events. The hosted Order/Payment matrix passed locally and against the Omise test account through pre-authorization/token handoff. Signed webhook recovery has a guarded live runner for an exact post-commit/pre-acknowledgement Payment kill, lease-expiry restart and signed duplicate replay. | Execute the live runner through a trusted external test callback; production acceptance, provider-originated redelivery observation, alert delivery, delayed policy, refunds, and settlement remain release/future work |
| POS | Product-owned shift/cash/terminal interfaces and PostgreSQL stores; authorization scopes; terminal-scoped cash replay; migration-4 immutable manual-tender projection and durable consumer; cash/PromptPay drawer invariants; WPF Paid control; terminal-bound SQLite schema-2 operational state/outbox and pre-send supervisor-decision recovery; joined SQLite-backed OIDC/WPF cash checkout acceptance; cashier/terminal reconciliation and version-fenced close; migration-5 exact-store closed-session history and immutable, idempotent supervisor variance review with Authorization-7 read/resolve separation, late-financial-version invalidation, atomic late-settlement total repair, and movement-derived recovery of legacy review rows; guarded local human supervisor recovery acceptance passed on 2026-09-15. | Add multi-store review/export and Reporting projection, extend synchronization to order/shift/device consumers, and complete physical-terminal supervisor and session-policy acceptance |
| Reporting | Product-owned read/query ports; tenant authorization; Customer BFF/API; durable audit projection; migrations 4-13 align Catalog/Media/Notification/Payment, Kitchen, Customer, Notification delivery, Payment recovery, and Order payment-review resolution vocabulary/replay | Keep projection compatibility/rebuild and rollback evidence current as producers expand |
| Restaurant | Product-owned hierarchy/configuration ports and repositories; tenant/platform policies; Platform Admin and Customer BFF/API contracts; audit integration events and outbox; reversible migrations | Extend the same pattern to future restaurant workflows without weakening organization-leading predicates |

Notification, Catalog, Inventory, Payment intent creation/capture recovery, Kitchen ticket lifecycle, and Customer profile creation are development-complete vertical slices in this phase. Payment's coordinated local PostgreSQL, RabbitMQ, migration, Reporting replay, restart recovery, duplicate-terminal, and partial-compensation retry evidence passed. The concrete-provider runner stops its exact server-side acceptance process after authorization and capture responses, then verifies recovery through Payment status lookup, RabbitMQ reconciliation, and a real Order host. Its complete local hosted-simulator run passed on 2026-09-16, including broker/Payment restart and cleanup. Its four-scenario Omise credentialed hosted run passed on 2026-09-17, and the subsequent five-scenario run passed pre-authorization token handoff on 2026-09-18. Production acceptance and alert delivery remain environment-specific gates. Full Catalog clean-install evidence remains a separate production migration release gate. The table deliberately leaves the overall phase marked partial because other product services retain exit work.

Notification provider delivery has the same durable boundary: PostgreSQL remains authoritative, expired submit/reconcile leases are recoverable, provider calls reuse Notification ID as their idempotency key, lifecycle writes are atomic with safe audit/events, and receipt polling is bounded. Seven component cases, the actual 0→3→2→3 runner, Reporting migration-7 replay, and the real HTTP boundary passed locally. A generic HTTPS bearer-token provider contract is covered; no public webhook is exposed without a concrete signed callback contract. See [Notification provider-delivery evidence](Evidence/Phase-10-Notification-Provider-Delivery.md).

Customer profile creation has the same implementation boundary, including conflict-safe concurrent replay, audit excluding profile fields, transactional publication, Reporting compatibility, and six successful coordinated acceptances. Unimplemented profile lifecycle operations are not implied by creation events. See [Customer profile-creation evidence](Evidence/Phase-10-Customer-Profile-Creation.md).

## Rollout order

1. Apply the owning service migration and any permission migration.
2. Deploy consumers disabled, then verify database connectivity, telemetry, and DLQ policy.
3. Enable consumers and prove duplicate delivery is harmless.
4. Deploy producers/outbox dispatchers only after consumers are compatible.
5. Enable BFF/UI routes last and run cross-tenant denial plus workflow acceptance tests.

Rollback reverses that order: disable the browser route and producers, drain/stop dispatchers and consumers, roll back application binaries, then execute reviewed database downgrade plans. Never downgrade while an incompatible producer is publishing.
