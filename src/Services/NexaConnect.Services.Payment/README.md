# Payment Service

The [five-scenario hosted Omise test-account gate passed on 2026-09-18](../../../docs/Architecture/Evidence/Omise-Five-Scenario-Recovery-Acceptance.md), including pre-authorization token handoff. Payment now implements opt-in signed test webhooks with migration-8 durable event-ID ingestion, authenticated canonical event validation and existing lease-fenced status-only recovery. Notifications supplement polling; they do not issue financial commands or replace current-charge validation. External signed delivery acceptance and production enablement remain outside this pass. See [configuration, failure recovery and telemetry](../../../docs/Deployment/Omise-Webhooks.md).

Opt-in Development-only `card_omise_test` checkout now forwards a masked, transient test token from POS to Order and Payment. Pending intents without a token await a fresh operator token; retries use the original Order identity and contents. Order reports `cardTokenRequired=true` only for that authoritative awaiting-token result; uncertain/started operations cannot enable replacement-token entry. Tokens never enter SQLite/outbox or durable workflow state. The opt-in five-scenario hosted Omise gate passed on 2026-09-18, including pre-authorization/token handoff. [Local POS Omise verification passed on 2026-09-18](../../../docs/Architecture/Evidence/Omise-POS-Card-Acceptance.md) with operator-attested UI/OIDC/sign-out; the verifier does not independently inspect those interactions. Production browser card collection remains planned. See the [configuration and acceptance guide](../../../docs/Deployment/Omise-POS-Card-Token-Handoff.md).

Omise hosted recovery acceptance is available through scripts/test-order-provider-recovery-live.ps1 -Adapter Omise, using four fresh tokens, real service processes and generated PostgreSQL/RabbitMQ infrastructure. Tokens never reach hosted recovery processes; Payment receives only its explicit test secret. Credentialed four-scenario hosted Omise acceptance passed on 2026-09-17; pre-authorization interruption and production acceptance remain outside this evidence. See [instructions and excluded boundaries](../../../docs/Deployment/Omise-Hosted-Recovery-Acceptance.md).


See [retained four-scenario hosted Omise evidence](../../../docs/Architecture/Evidence/Omise-Hosted-Recovery-Acceptance.md) for the exact pass and exclusions.

A Development-only local HTTPS Omise token helper is available at `src/Tools/NexaConnect.OmiseTokenHelper`. It uses official browser tokenization with the operator's test public key and a fixed test card, keeps tokens out of server requests and logs, and replaces the documentation demo for acceptance preparation. Trusted development HTTPS is required; direct credentialed Omise acceptance passed on 2026-09-17; Development-only POS/Order test-token handoff is implemented; local POS verification passed on 2026-09-18 with operator-attested UI/OIDC; independent proof of UI interaction and production card collection remain outside this pass. See the [Omise acceptance guide](../../../docs/Deployment/Omise-Test-Account-Acceptance.md).

Omise test-account support is available with PaymentProvider__Adapter=Omise and secret-injected PaymentProvider__OmiseSecretKey. Only Development/Testing and test keys are accepted; endpoint and TLS are fixed to the official API. The Order-only authorize route accepts an ephemeral cardToken body, with missing input rejected before a pending intent acquires a lease. Existing workers/outbox persist recovery; uncertain authorization uses metadata search to find the original charge; missing or ambiguous matches remain unknown. No schema change is needed. The opt-in Development-only POS/Order `card_omise_test` path now forwards a transient token; manual tender remains the default. Production browser card collection remains planned; local POS verification passed on 2026-09-18 with operator-attested UI/OIDC steps. Run `scripts/test-payment-omise-live-sandbox.ps1 -ConfirmSandboxTransactions` with two fresh test tokens. The runner first builds with `--no-restore`, retains `.runstate/payment-omise-live/<run-id>/build.log`, and then runs the credentialed test with `--no-build`. A build-only failure makes no provider request and consumes no token; inspect the compiler/MSBuild error in that log. Direct credentialed acceptance passed on 2026-09-17; four-scenario hosted Omise acceptance passed on 2026-09-17; the subsequent pre-authorization/token-handoff gate passed on 2026-09-18. See [setup, failure modes, observability and gate scope](../../../docs/Deployment/Omise-Test-Account-Acceptance.md).

The provider recovery gate now includes hosted pre-capture void recovery, duplicate delivery and paid-Order protection. Its five-scenario local hosted acceptance passed on 2026-09-17, including duplicate void delivery and cleanup; four-scenario hosted Omise execution passed on 2026-09-17; five-scenario Omise pre-authorization acceptance passed on 2026-09-18; production acceptance remains open. See [recovery scope, evidence and remaining release gates](../../../docs/Architecture/Evidence/Phase-10-Order-Workflow-Recovery.md).

The provider recovery harness provisions a durable `payment.#` evidence subscription before any Payment host starts, then receives the exact terminal event with its persistent flag after restart. Order's separate queue handles reconciliation and still must complete the matching inbox message. This fixes unroutable normal terminal-event publication in the acceptance broker without weakening mandatory publisher confirmation.

Without external provider credentials, use `scripts/test-order-provider-recovery-local.ps1 -ConfirmDisposableInfrastructure -ConfirmProcessTermination`. It supplies an acceptance-only HTTPS authorization/capture/void/status simulator and drives the same real hosted matrix. `PaymentProvider__SimulatorCertificateSha256` is an internal exact certificate pin allowed only in Testing against IPv4 loopback HTTPS; the ordinary provider handler retains platform certificate validation and deployed environments reject this setting. No Windows trust store is modified. See the [simulator guide](../../Tools/NexaConnect.PaymentProviderSimulator/README.md); the complete local hosted matrix passed on 2026-09-16, including all three interruption scenarios and cleanup.

Owns payment intents, provider authorization state, and idempotency keys. `POST /api/payment/v1/intents`, `POST /api/payment/v1/intents/{id}/authorize`, and `GET /api/payment/v1/intents/{id}` provide the implemented payment boundary. Every request requires `X-Nexa-Organization-Id`; only the exact `nexaconnect-order-service` workload can invoke authorization. Other globally trusted workloads are denied. Order propagates server-owned organization/restaurant/branch context and customer create/read calls additionally use the protected application context. Set `Persistence:Provider=PostgreSQL` and `ConnectionStrings:Payment` to use organization-leading persistence; development defaults to the non-durable in-memory adapter. Reusing an organization/restaurant idempotency key returns the original intent only when the normalized request matches and otherwise returns `409`.

Customer Portal payment-intent calls carry the shared tenant context. Payment validates Platform Directory access, referenced Order ownership, Restaurant scope, and `payment.intent.create` or `payment.intent.read`. Cross-tenant reads return `404`. Configure Platform Directory, Order, Restaurant, Authorization, and workload credentials.

Payment migration 1 owns the outbox. Migration 2 adds `organization_id`, append-only `payment_audit_records`, and tenant-leading intent lookup. Migration 3 adds authorization state and migration 4 adds recoverable lease ownership, bounded attempts, and reconciliation timestamps. Reporting migrations 8 and 9 must be applied before authorization publication. A new intent and each authorization transition atomically commit local audit plus versioned lifecycle and `payment.audit.v1` outbox messages. The provider call occurs after the start transaction commits and before a separate result transaction; no database transaction spans network I/O. Enable dispatch with `Outbox:Enabled=true` and secret-managed `Outbox:ConnectionString`.

Migration 2 marks pre-existing intents with the empty organization UUID because Payment cannot infer ownership from its database. Before deploying the migration-2 application or enabling dispatch, backfill every sentinel row from Order-owned organization data through a reviewed operational reconciliation and verify none remain. Before downgrade, verify no `(restaurant_id,idempotency_key)` is used by multiple organizations; the downgrade fails safely until collisions are reconciled.

Authorization uses the payment-intent ID as the stable provider request identity. One caller atomically acquires the authorization lease; concurrent/replayed calls observe `authorizing` or the terminal result without a second provider call. Only sanitized provider references and allow-listed machine failure categories are retained. Provider response bodies, PAN, CVV, tokens, and secrets must never enter persistence, events, audit, or logs. A timeout or unavailable provider is recorded as `unknown`, not failed. The hosted recovery worker reclaims expired leases using PostgreSQL row locks, looks up provider status with the stable Payment intent ID, and atomically records `authorized`, `failed`, or `requires_action`. Exhaustion during claim recovery also emits the reconciled event in the same transaction, using the claim correlation ID, so Order can move the bound PaymentPending aggregate to PaymentReview.

The immediate-capture boundary adds Order-only `POST /api/payment/v1/intents/{id}/capture`. Only `authorized` can move to `capturing`; provider I/O occurs outside the database transaction, and a separate transaction records `captured`, `failed`, or `capture_unknown` with safe audit and outbox events. Order becomes paid only for `captured`; uncertain capture remains payment-pending without Inventory/Kitchen compensation. Provider capture references are internal. Capture uses the Payment intent ID as `Idempotency-Key`, so the configured provider must enforce stable replay semantics across transport retries.

Payment migration 6 adds capture-specific recovery leases, bounded attempts, and reconciliation timestamps. The PostgreSQL-only recovery worker claims expired `capturing` and `capture_unknown` intents, calls the provider status endpoint without holding a database transaction, and transactionally records `captured`, `failed`, `capture_unknown`, or `requires_action` plus `PaymentCaptureReconciledV1` and `payment.audit.v1`. It never repeats the capture command. Only an explicit provider `captured` result completes Order; explicit failure triggers Order-owned idempotent Inventory/Kitchen compensation, while unknown or exhausted outcomes retain the order for review. Apply Reporting migration 11 before enabling recovery publication. Configure `PaymentProvider__CapturePath`, `PaymentProvider__CaptureStatusPath`, `PaymentProvider__LeaseDuration`, `PaymentProvider__MaximumCaptureRecoveryAttempts`, and `PaymentProvider__RecoveryInterval`.

The void boundary exposes exact-Order-workload-only `POST /api/payment/v1/intents/{id}/void`, uses `void:{intent-id}` for provider idempotency, and rejects captured payments. Payment migration 7 persists sanitized provider void references, leases, bounded attempts, and reconciliation timestamps. Provider calls and status lookups remain outside database transactions; each local transition atomically appends safe audit plus versioned void/outbox events. Recovery never repeats an uncertain void command and moves exhausted uncertainty to `requires_action`. Apply Order migration 3 and Reporting migration 12 before publication. Order validates persisted organization and payment-intent ownership before applying an outcome. Configure `PaymentProvider__VoidPath`, `PaymentProvider__VoidStatusPath`, `PaymentProvider__MaximumVoidRecoveryAttempts`, and `PaymentProvider__VoidRecoveryEnabled`. Local PostgreSQL evidence passed successful transactional unknown-to-voided recovery, Payment `0→7→6→7`, and Order `0→3→2→3`; live RabbitMQ void replay remains a follow-up gate. Delayed capture policy, refunds, and settlement reconciliation remain planned.

Payment starts with `PaymentProvider__Adapter=Disabled`. The fail-closed adapter performs no network operation: authorization/capture/void commands return the bounded `provider_not_configured` failure, status lookups remain unknown, and PostgreSQL recovery workers are not registered, so existing uncertain work is not consumed while no provider is selected. Select `GenericHttp` only after configuring and validating a provider-compatible endpoint.

The generic HTTP provider adapter requires an absolute HTTPS `PaymentProvider__BaseUrl`, applies `PaymentProvider__RequestTimeout` (default 15 seconds; maximum two minutes), and can inject a secret-managed bearer credential through `PaymentProvider__ApiKey`. Never place the key in checked-in configuration. Capture/void timeouts, transport failures, HTTP 408/429/5xx responses, and malformed successful payloads are classified as uncertain and therefore require status reconciliation; 4xx integration failures are definitive provider failures, not card declines. Provider failure text and response bodies are not persisted or logged. Run `./scripts/test-payment-provider-sandbox.ps1 -ConfirmLocalSandbox` for the 18-case synthetic contract suite and sanitized JSON/TRX evidence. This verifies disabled-mode behavior, authentication injection, stable idempotency keys, bounded failure categories, rate-limit handling, and malformed/timeout behavior.

For a real provider sandbox, inject `NEXACONNECT_PAYMENT_PROVIDER_SANDBOX_URL`, `NEXACONNECT_PAYMENT_PROVIDER_SANDBOX_API_KEY`, two distinct disposable pre-authorized references (`...CAPTURE_AUTHORIZATION_ID` and `...VOID_AUTHORIZATION_ID`), `...SANDBOX_AMOUNT`, and `...SANDBOX_CURRENCY`; optionally override the four command/status path variables. Run `./scripts/test-payment-provider-live-sandbox.ps1 -ConfirmSandboxTransactions`. The script rejects non-HTTPS and production-looking hosts, URL user information/query/fragment, and duplicate authorization references; it requires exactly one unskipped acceptance test. It proves the account's TLS handshake, acceptance of the configured bearer credential, capture/void replay identity, and status reconciliation. It does not prove that anonymous/invalid credentials are rejected and does not manufacture rate limiting or a lost response. Those gates require provider-specific, non-locking fault controls. Successful evidence under `.runstate/payment-provider-live/<run-id>/` intentionally contains no credential or authorization reference; failed console/TRX output can contain endpoint diagnostics and must be access-restricted and reviewed before sharing.

Order's end-to-end provider recovery gate is `scripts/test-order-provider-recovery-live.ps1`. Its default `-Adapter GenericHttp` path uses the same URL/key, amount/currency, and optional authorization/capture/void command/status paths, but creates five new `card` authorizations, three captures and two pre-capture voids rather than consuming pre-authorized references. With all three confirmation switches, it interrupts the server-side acceptance process after intent creation, after the provider authorization response, and after the provider capture response, and after pre-capture void responses for pending and paid-protection Orders. For each scenario it then starts real Order and Payment hosts, stops RabbitMQ, lets Payment's hosted recovery worker commit the authoritative state and an unpublished transactional-outbox message, terminates that exact Payment process, restarts RabbitMQ and Payment, and verifies outbox publication plus Order inbox consumption. Recovery must use provider status for uncertain responses and preserve one Payment intent; capture scenarios finish captured/Paid, while void scenarios finish voided with the pending Order failed or the already-paid Order unchanged. Follow the exact procedure in the [deployment guide](../../../docs/Deployment/Deployment-Guide.md); the complete local hosted-simulator matrix passed on 2026-09-16; credentialed four-scenario hosted Omise acceptance passed on 2026-09-17; five-scenario Omise pre-authorization evidence passed on 2026-09-18; production evidence remains required.

The acceptance supplies only a loopback OIDC/dependency fixture; Payment HTTP, hosted recovery scheduling, PostgreSQL persistence, transactional outbox dispatch, RabbitMQ transport, Order reconciliation consumption, and workload authorization use production host code. The runner records the broker outage before Payment recovery, requires a persisted unpublished terminal event with a failed publish category, kills Payment only after that commit, and accepts completion only after the restarted host publishes the same event and Order records the matching inbox message. Sanitized evidence excludes identifiers and secrets. The complete local hosted matrix passed on 2026-09-16 after correcting restart-persistent broker storage and the lifecycle evidence subscription; retained evidence covers three provider-boundary interruptions, three Payment-host interruptions, exact persistent terminal delivery, real Order inbox completion and cleanup. Four-scenario hosted Omise evidence passed on 2026-09-17; five-scenario Omise pre-authorization evidence passed on 2026-09-18; production evidence remains open.

Structured logs use service name `nexaconnect-payment`; correlation IDs propagate to Platform Directory, Order, Restaurant, and Authorization.
JSON stdout is always enabled. Enable OTLP with `Observability__OtlpEnabled=true`; use the [observability guide](../../../docs/Deployment/Observability.md) for the endpoint and queries.

Anonymous `GET /health/live` reports process liveness. `GET /health/ready` additionally checks PostgreSQL reachability and Payment migration 7 when PostgreSQL persistence is configured; it intentionally does not call the payment provider. The `nexaconnect-payment` meter includes capture-recovery claims, outcomes, bounded failure categories, a worker-registration/export-presence gauge (not a heartbeat), capture-recovery backlog/oldest-age gauges, unpublished-outbox count/oldest-age gauges, and a collection-failure counter. Void recovery currently emits safe structured failure logs; dedicated void gauges and alerts remain operational hardening. A collection failure means the last gauge samples may be stale. `OperationalMetrics:PollInterval` controls the PostgreSQL poll interval and defaults to 30 seconds.

Capture recovery is enabled by default. During a controlled provider incident or rollback rehearsal, set `PaymentProvider__CaptureRecoveryEnabled=false` and restart Payment to pause capture-status lookups without disabling authorization recovery or altering durable capture state. Follow the [capture-recovery runbook](../../../docs/Deployment/Payment-Capture-Recovery-Runbook.md) before using this control.
Capture recovery also exports claim, outcome, and failure counters through meter `nexaconnect-payment`. Operational thresholds and safe recovery procedures are documented in the [capture-recovery runbook](../../../docs/Deployment/Payment-Capture-Recovery-Runbook.md).

Omise charge response currency is validated case-insensitively against THB (both documented uppercase and lowercase are accepted); other currencies remain rejected. The live gate reports only local authorization state and a normalized failure category on authorization failure, without provider payloads or identifiers. A failed first authorization does not establish whether a charge exists: inspect test-dashboard charges before starting a new run with fresh tokens.

HTTP 400 diagnostics now retain only fixed categories mapped from recognized Omise error codes (for example provider_token_used or provider_feature_unsupported). Provider messages, arbitrary codes and payloads are discarded; error reads are capped and timed out. These categories never convert uncertainty into financial success/failure and do not trigger retries. See the Omise acceptance guide troubleshooting section.

Read-only existing-charge diagnostics are available through `scripts/inspect-omise-test-charge.ps1`. It uses hidden charge-reference/secret input and exactly one official Omise GET, reports explicit nullable flags/minor-unit amounts and validation booleans, and never invokes authorization/capture/reversal/refund or local reconciliation. The identifier-free report is operator output, not financial telemetry or acceptance/ownership evidence. See the [existing-charge inspection guide](../../../docs/Deployment/Omise-Test-Account-Acceptance.md#inspect-an-existing-charge-without-another-payment).

Omise full manual capture now explicitly requests final_auth at authorization. If a paid response omits captured_amount, confirmation requires returned final_auth plus successful/authorized/unreversed/deferred/non-capturable/non-reversible state, zero refunds and exact intent amount/currency/metadata binding. Partial/conflicting amounts remain unknown; an absent authorization type requires the verified signed creation context described below. Read-only diagnostics include bounded AuthorizationType and retain the raw captured amount as null; no value is invented. Existing unmarked Paid charges cannot gain this evidence retroactively.

For responses omitting both captured_amount and authorization_type, new authorizations include a versioned HMAC-bound full-capture creation context in provider metadata. Strict flags, zero refunds and local intent binding remain required; invalid or modified context and partial/type conflicts cannot override them. The proof attests to request context, not a unique provider charge or local ownership. Capture explicitly sends the exact capture_amount. Existing unmarked Paid charges stay unknown and receive no POST. Inspection reports only TrustedFullCaptureContextVerified, never a proof/key. Changing the test secret invalidates old proofs; resolve recovery before rotation. See [ADR-008](../../../docs/Architecture/Decisions/ADR-008-omise-full-capture-context.md) and the [compatibility guide](../../../docs/Deployment/Omise-Test-Account-Acceptance.md#responses-omitting-both-capture-amount-and-authorization-type); credentialed context retention passed in the direct test-account gate on 2026-09-17.

Local webhook regression: start local Compose PostgreSQL, restore dependencies, then run `pwsh -NoProfile -File scripts/test-payment-omise-webhook-local.ps1`. The guarded local runner requires 133 unit, 9 HTTP and 4 isolated-schema PostgreSQL cases without skips and retains a safe summary. Run `6a02840b0a1449098f39ef2282825e62` passed on 2026-09-18 with no provider requests, financial commands or product-data changes. Simulated lease recovery passed; external signed delivery and process-kill acceptance remain unverified. See the [procedure and evidence scope](../../../docs/Deployment/Omise-Webhooks.md#local-regression-verification).
