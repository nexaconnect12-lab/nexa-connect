<Window x:Class="NexaConnect.POS.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="NexaConnect POS" Height="260" Width="520"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="NexaConnect POS" FontSize="24" FontWeight="SemiBold" />
        <TextBlock Grid.Row="1" Margin="0,12,0,18" TextWrapping="Wrap"
                   Text="Sign in with your NexaConnect account to open a secure POS session." />
        <StackPanel Grid.Row="2">
            <Button Width="140" HorizontalAlignment="Left" Content="Sign in"
                    Click="SignIn_Click" />
            <TextBlock x:Name="StatusText" Margin="0,14,0,0" TextWrapping="Wrap" />
        </StackPanel>
    </Grid>
</Window>
